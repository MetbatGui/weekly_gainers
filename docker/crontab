# 평일(월~금) 15:50 KST에 sync(주간+월간 갭백필+수집+Drive 동기화) 실행.
# KRX 장마감(15:30) 이후 시세 확정 대기 버퍼로 15:50에 잡음.
#
# root로 실행 - /proc/1/fd/1(컨테이너 stdout)이 root:root 전용 권한이라
# nonroot로 돌리면 리다이렉션 자체가 Permission denied로 실패해 잡이 아예 안 돎.
50 15 * * 1-5 root /app/docker/run-sync.sh >> /proc/1/fd/1 2>&1
